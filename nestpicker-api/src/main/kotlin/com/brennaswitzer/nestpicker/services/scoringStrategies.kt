package com.brennaswitzer.nestpicker.services

/**
 * Ranking Scale is from 1 - 10 stars for now until there is a compelling reason to change it
 * This is a good place to make sure that it can be adapted since I might
 * need to change it
 */
fun costOfLiving(value: Int): Double {
    return 0.1
}
